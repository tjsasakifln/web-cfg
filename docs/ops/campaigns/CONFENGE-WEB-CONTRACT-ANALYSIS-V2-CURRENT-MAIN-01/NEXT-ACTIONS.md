# Next actions

1. Wait for required checks `site-ci` and `pSEO quality gates` on the replacement PR.
2. Merge only if those checks are green, the PR is not behind `origin/main`, review threads are empty, `index_count=1`, and loc sets still reconcile.
3. After merge: confirm `origin/main` SHA, Netlify `commit_ref`/`deploy_id`, fetch the live URL twice.
4. Comment and close PR #157 as `superseded_by=<replacement PR>`.
5. Leave issue #83 open. Publication is not live DoD.
6. IndexNow stays prepare-only. A receipt is not indexation.
7. Do not claim Google indexed or ranked the URL.
8. Do not open a second analysis, município page, or pSEO factory.
9. Measure citation/qualified engagement before any EXPAND recommendation.
