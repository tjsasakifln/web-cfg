# CFG10X-12 — design system before/after

Branch: `goal/cfg10x-12-design-system`. Baseline: `origin/main` @ 1979aaa4.

## What changed

- Tokens for type floors live in `styles-tokens.css` (`--text-body-mobile: 1rem`, `--text-micro: .8rem`).
- Layout contracts and the 12.8px microcopy floor are modules in `css/`, concatenated last into `styles.css`.
- Geometry suite measures computed `font-size` on home, entregas, artigo, oferta, ferramenta, casos and especialista at 390/768/1024/1440.
- Historical 9.76px prova collapse is a failing fixture: `scripts/site/fixtures/type-floor/`.

## Bundle

| | before | after |
| --- | --- | --- |
| `styles.css` | 115318 B | 112642 B (−2676) |
| public CSS total (7 sheets) | 180190 B | 177352 B |
| sub-0.8rem font-size literals on those sheets | 116 | 0 |
| CSS framework | none | none |

Navy/verde tokens are unchanged.

## Screenshots

Before (main, already in the repo): `docs/uiux-evidence/cfg10x-12-before/`
After (this branch): `docs/uiux-evidence/cfg10x-12-after/`

Families at 390×844 and 1440×1000: home, entregas, artigo (`conteudos/documentos-reequilibrio-obra-publica`), oferta (`diretoria-b2g`), ferramenta (`ferramentas/diagnostico-defesa-margem`), casos, especialista.

## Collisions

Started from current `main`, not from in-flight CFG10X route PRs. Route PRs that restyle the same selectors (hero-proof, evidence, catalog type) will need to rebase onto this floor instead of reintroducing `.61rem`/`.73rem`.
