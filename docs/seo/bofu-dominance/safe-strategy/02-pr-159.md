# PR #159 note — does not authorize HTML edit of these three URLs

PR: https://github.com/tjsasakifln/web-cfg/pull/159  
Title: `feat(revops): operational SEO demand-control loop (no content factory)`  
State: open, **draft=true**, not merged.  
Head: `campaign/confenge-web-seo-demand-control-02` @ `feba68928ab997229028a66bb25d3b3b5a439206`  
Base: `main` @ `faadc16609210522c9ffaf32a7b944817f6c6214`

## `authorizes_html_edit=false`

PR body: next-action queue is recommendation only. `data/organic/demand-control-queue.json` is declared with **`authorizes_html_edit=false`**. That file is not on `origin/main` in this workspace (PR #159 is unmerged). The PR itself is the authority for the flag.

## observe_only BOFU set is the #128 pillars, not these three URLs

Queue item 2 in the PR body:

> **observe_only** BOFU pillars — `/aditivos-obras-publicas/` — exclusion #128

Together with:

1. observe_only CTR gap — `/conteudos/sinapi-desonerado-nao-desonerado/` — exclusion #126
3. observe_only noindex canary — `/conteudos/chuva-prorrogacao-prazo-obra-publica/` — exclusion #127

PR body also states HTML/copy/H1/title/meta/CTA/internal links/robots were **not** changed, and SINAPI snippet (#126), BOFU pillars (#128), noindex canary (#127) stay in observation.

The three URLs in this campaign are **not** in that observe_only set:

- `/bid-room-licitacoes-obras/`
- `/diretoria-b2g/`
- `/diagnostico-b2g-expansao/`

#159 therefore does not freeze these three pages, does not authorize editing #128 pillars, and does not authorize ranking claims. This campaign edits only the three unfrozen offer URLs plus this pack and exclusive tests.

Current live GSC overlay is `LIVE_JOB_OK` with `core_ready_for_product_decisions=false`. PR #159 freeze-head historically recorded `credential_failure` / `NEEDS_EXTERNAL_SECRET`; that is not the current mechanical state. Absence is not zero. This census does not use GSC positions as evidence of dominance.
