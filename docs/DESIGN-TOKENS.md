# Design tokens (Story 1.9)

**Single source:** `styles-tokens.css` (`:root` palette + layout primitives).

Imported by:

- `/styles.css`
- `/styles-tools.css`

Naming: `--navy-*`, `--green-*`, `--ink`, `--text`, `--muted`, `--line`, `--soft`, `--container`, `--radius`, `--shadow`.

Do not introduce a second primary palette. See `docs/DESIGN-SYSTEM.md` and `data/site/design-system.json`.
