# PR #457 port sobre o main de 2026-08-29

## Decisão e base

- Estado: `EXECUTE_NOW`.
- Frente executiva: `INBOUND ENGINE`.
- Base protegida: `72ed3831ba28c9400627cdc9599aa54e9329e178` (`origin/main` após `git fetch --all --prune`).
- Branch: `goal/10x10-public-tools-20260829`.
- PR de origem auditado: #457, branch `goal/cfg10x-18-tools-product`, dois commits e nove commits atrás do main no momento do porte.
- Estratégia: porte dos dois commits válidos para worktree novo, seguido de correções TDD. A worktree antiga estava fortemente suja por trabalho concorrente e não foi alterada.
- Tempo até evidência: no mesmo ciclo de implementação, por fixtures determinísticas, browser real e artefato `_site`.
- Alavancas: confiança, conversão, automação e distribuição.

Repetir o fluxo cem vezes melhora o sistema: os mesmos contratos de compute, privacidade, CSP, teclado, axe, overflow e conversão verificam todas as execuções. Não há produção manual de cem páginas nem uma segunda fonte de verdade.

## Visitor jobs e hipótese comercial

| Superfície | Trabalho do visitante | Resultado antes do cadastro | Ponte posterior |
| --- | --- | --- | --- |
| Limite de acréscimos e supressões | Conferir o recorte numérico do art. 125 | Saldos independentes, arredondamento e premissas | CTA compatível com aditivos |
| Checklist de reequilíbrio | Identificar prontidão e bloqueadores documentais | Escore defensável, lacunas e ordem de correção | CTA compatível com reequilíbrio |
| Matriz de atraso | Estruturar eventos e lacunas de nexo/prova | Hipóteses preliminares, nunca veredito | CTA compatível com atraso/prorrogação |
| Diagnóstico de defesa de margem | Ler fatos públicos e UNKNOWN de um contrato | Identificação, resumo, timeline, fontes e limites | Formulário de segunda leitura após o resultado |

Hipótese: entregar utilidade verificável antes da captura aumenta confiança e qualifica a oportunidade comercial sem transformar o cálculo em promessa jurídica. O resultado não depende do CTA; o CTA recebe somente o contexto de próxima ação compatível.

## Dados, provenance e autoridade

- Fatos, identidade e provenance do diagnóstico continuam vindo do contrato versionado SELECT-only `public-read-margin-defense/1.0`, produzido fora deste runtime sob autoridade do `extra-cli`.
- O `web-cfg` apenas projeta o snapshot público e preserva `UNKNOWN`; não cria crawler, DataLake, identidade ou conclusão jurídica paralela.
- A captura persiste `source=CONFENGE_WEB` e o contexto normalizado de próxima ação. Warmbly continua dono do handoff/outcome.
- Aprovações, exceções e intervenções continuam sob Governance/Control Center.
- ADR-STRAT-002 e RUNTIME-AUTHORITY não mudam: CONFENGE é a única superfície pública e nginx/Netcup permanece produção.

## Correções adicionais sobre o PR original

### Standards review

- Corrigido o E2E que encerrava com sucesso quando o Chrome não abria. O gate agora falha com `CHROME_UNAVAILABLE`, coberto por subprocesso determinístico.
- Corrigida a atribuição falsa em que qualquer clique dentro da seção comercial contava como clique de oferta. A instrumentação agora é delegada ao alvo acionável.
- Centralizado no browser um allowlist tipado de propriedades analíticas; strings livres, identificadores, CNPJ, dinheiro e chaves desconhecidas são descartados antes do tracker.
- Removida a tabela `TOOL_JOBS` não consumida; o visitor job verificável permanece no HTML publicado e no registro de família.

### Spec review

- Acrescentados casos determinísticos de propriedade para reequilíbrio e matriz, além do limite aditivo.
- Todos os itens `N/A` e estados fora do domínio deixam de produzir prontidão média; retornam erro explicável e sem CTA/export.
- Datas impossíveis, período invertido, duração negativa/overflow e sobreposição derivada de início+duração são tratados explicitamente.
- Erro e limpeza do diagnóstico removem resultado e identificadores obsoletos.
- Os quatro fluxos são concluídos somente por teclado; os quatro estados sem JavaScript são fail-closed e sem query string.
- Axe passou também nos resultados dinâmicos, não apenas no carregamento inicial.

## Evidência medida

- Artefato testado: `_site` (`PUBLIC_ARTIFACT_REQUIRED=1`).
- Viewports: 320x568, 360x800, 390x844, 430x932, 768x1024, 1024x768 e 1440x1000.
- Overflow horizontal: 0 em 42 combinações de rota/viewport.
- Axe: critical 0, serious 0, moderate 0 e minor 0 nas seis superfícies iniciais e nos quatro resultados dinâmicos.
- Teclado: conclusão integral nas quatro ferramentas.
- CSP: 262 HTMLs, 234 blocos inline, 24 hashes únicos; canário real em seis rotas com zero violação.
- SEO: 260 páginas, 75 no sitemap, 75 indexáveis, zero erro e zero warning.
- Build público: 492 arquivos, auditoria do artefato sem findings, visible parity em 75 páginas sem defeitos.
- Runtime: `confenge-nginx-node/v2`, `HTTP_SEO_PARITY_GATE_READY / NETCUP_CANDIDATE_NOT_YET_PROMOTED`.

Relatórios reproduzíveis: `evidence/e2e-report.json`, `evidence/axe-report.json`, `evidence/e2e-summary.txt` e `evidence/screenshots/after/`.

## Gates executados

| Gate | Resultado |
| --- | --- |
| `npm run test:tool-compute` | verde, fixtures e properties determinísticos |
| `npm run test:tools` | verde, inclui browser indisponível fail-closed |
| `npm run test:diagnose-margin` | verde, SELECT-only e loop comercial persist-first |
| `npm run test:analytics` | verde, allowlist agregada vazia e rejeição de PII |
| `npm run test:copy` | verde |
| `npm run validate:seo` | verde, 0 erros/0 warnings |
| `npm run inbound:gates` | verde, 0 erros; dívidas nominais preexistentes reportadas |
| `npm run build:site` | verde |
| `python3 scripts/site/test_csp_contract.py` | verde |
| `npm run test:csp-browser` | verde, 0 violações |
| `npm run test:runtime-authority` | verde, 23/23 |
| `PUBLIC_ARTIFACT_REQUIRED=1 npm run test:tools-uiux-e2e` | verde, 0 failures |

O inbound gate continua reportando as exceções route-exact existentes para limite, reequilíbrio e matriz, sob issue #61 e expiração em 2026-09-30. Este porte não ampliou allowlist nem prazo. A ponte comercial pós-resultado está presente; a dívida nominal é a ausência de captura terminal na própria rota.

## Analytics, segurança e rollback

- Analytics registra apenas taxonomia e contagens limitadas. Identificador de contrato, busca, texto livre e valores monetários não são enviados.
- Valores e respostas persistem somente em `localStorage` versionado; share/export não os coloca em URL.
- O diagnóstico escapa conteúdo do contrato e restringe links de fonte a HTTP(S).
- CSP segue sem `unsafe-inline` em `script-src`; hashes foram recalculados a partir do artefato.
- Rollback: reverter os commits desta branch. Não há migração de dados, URL nova, redirect, mudança canônica ou mutação no produtor; as rotas atuais permanecem estáveis.

ADR afetado: nenhum contrato arquitetural foi alterado. ADR-STRAT-002 e RUNTIME-AUTHORITY foram revalidados e preservados; não há plano de produção, storage ou rollback Netlify.
