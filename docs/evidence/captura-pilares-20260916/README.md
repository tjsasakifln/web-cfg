# Captura on-page nos seis pilares B2G — evidência de publicação (2026-09-16)

Decisão do fundador EXECUTE_NOW (registrada em #61 e em `unlock-plan.capture.authorization`).

- **PR:** #695 (`campaign/b2g-pillars-capture-20260916`, HEAD `56aadc64b`) → merge commit `47da03b64ba701016eec296234b1d14225ebe588` em `main`.
- **Release:** `netcup-release` run 35127635610 — preflight, `site-ci` sobre o SHA de merge (a 1.ª execução reprovou em `/especialista/tiago-jun-sasaki/` por um pico de TBT 278 ms em 1 de 3 execuções com `longest_own_task_ms: 0`, mesmos bytes que mediram 100/100/100 na PR; re-execução dos jobs reprovados: perf 100 ×3), pSEO, pacote/atestação, stage, qualificação, **promoção atômica ✓, aceite público ✓** (535/535 HTML, 289/289 não-HTML, 78/78 sondas de contrato, robots 234/234), Lighthouse oficial na borda `MEASURED_PASS` (perf 100 ×3, LCP 1,57–1,59 s).
- **SHA público observado:** `build-info.commit` = `runtime-info.release_sha` = `47da03b64…`; `/healthz`, `/ready` 200; CSS `styles.93863fcc71c5.css` `text/css`; fonte `font/woff2`.
- **Seis páginas em produção (navegador real, cache desativado, 1440 e 390):** formulário `#captura-pilar` → `/.netlify/functions/lead` com `origem/asset_id/cta_id/route_family/jornada/estagio/landing_page` corretos, Turnstile presente, nav promovida (`Serviços e problemas | Obras públicas | Biblioteca`), CTA do cabeçalho `/triagem-tecnica/` (nenhum link ao formulário da home), sem cartão raster, logo canônico, WhatsApp preservado, Archivo carregada, sem overflow, ordem de tabulação nome→empresa→e-mail→WhatsApp→contexto→consentimento→botão, envio vazio bloqueado pela validação nativa. Capturas: `*-fold-prod.jpg`, `*-form-prod.jpg`.
- **Persistência (sonda sintética autorizada, executada no host com as credenciais do runtime, sem copiá-las):** seis execuções, uma por pilar com a atribuição da página — `TRANSPORT_READY`, 19/19 verificações: 201 na criação, 200 idempotente na repetição, recibo presente, registro `synthetic` excluído das métricas comerciais, Warmbly entregue exatamente uma vez com `auto_send:false` e `dispatch_attempted:false`, nenhum e-mail/notificação. Hashes de recibo em `synthetic-probe-47da03b64.json`.
- **Desempenho:** artefato servido com gzip (condição do orçamento): LCP 1,80 s nas seis (antes da campanha, produção: 2,3–2,7 s); perf 99–100; CLS 0. Borda (mesmo método, sem folga de rede): 1,95–2,12 s, contra 2,3–2,7 s antes.
- **Débito resolvido:** os seis débitos route-exact retirados do registro (`missing_on_page_form` = 0); `unlock-plan.capture.current_coverage` = 6. Pendências restantes do #61 (outras rotas: `/casos/aditivo-art125-demonstrativo/`, `/casos/medicao-glosa-demonstrativo/`, `/ferramentas/matriz-atraso-obra/`, `/metodologia-inteligencia/`) não fazem parte desta decisão e continuam registradas.
- **Não medido:** conversão, percepção humana, campo (INP/LCP reais).
