# Pedidos do lote A ao integrador

Nenhum pedido bloqueia o lote: todas as rotas foram fechadas com o CSS existente. Os itens abaixo
são melhorias ou limpezas em arquivos do integrador.

| # | Arquivo do integrador | Seletor / trecho | Página(s) | Motivo |
| --- | --- | --- | --- | --- |
| 1 | `quantitativos-orcamento-obras/index.html` | `<!-- plate:recorte-banheiroNone -->` | `/quantitativos-orcamento-obras/` | O marcador do slot não casa com a regex de `scripts/demonstrative/plates/inline.py` (`<!-- plate:([a-z0-9-]+)( eager)? -->`), então `inline --check` passa vazio para essa rota e a prancha não é re-materializada se o SVG mudar. Corrigir para `<!-- plate:recorte-banheiro -->` (o `<picture>` já materializado é o correto). |
| 2 | `scripts/demonstrative/plates/build_manifest.py` | `PLATE_META` | manifesto de ativos | Roteiro fixo com P1–P4. As seis pranchas do lote (`interferencia-verga-viga`, `revisao-conferencia`, `drenagem-rede`, `inspecao-fachada`, `pericia-fachada`, `sst-canteiro`, códigos PA–PF) estão em `assets-lote-a.json` com origem, licença e hash; acrescentar ao manifesto ou derivar `PLATE_META` de `render_plates.PLATE_SOURCES`. Os códigos usam letras porque o gate de numerais (`test_visible_numbers_come_from_the_sources`) rejeita "P5"…"P10". |
| 3 | `parcerias-engenharia/styles.css` e `data/design/css-usage-baseline.json` | `.partner-hero`, `.partner-hero-grid`, `.partner-lead`, `.partner-signal`, `.partner-steps`, `.partner-step`, `.partner-contact`, `.partner-contact-grid`, `.channel-card`, `.channel-actions` | `/parcerias-engenharia/` | A recomposição usa `svc-open`, `steps`, `conditions` e `sec--dark`; só `.partner-kits`, `.partner-kit*`, `.partner-share*`, `.partner-summary` e `.partner-print-note` continuam em uso. Podar as regras órfãs quando o baseline de uso de CSS for recapturado. |
| 4 | `styles-offers.css` | heroi de `/comercial/radar-decisorio/` (fundo em degradê claro) | `/comercial/radar-decisorio/` | O contrato de design proíbe degradê decorativo; a rota é oferta paga com formulário, preço e medição de primeira dobra, por isso ficou PRESERVADA_COM_JUSTIFICATIVA. Se o integrador quiser trazê-la ao vocabulário, trocar o fundo por `--soft` na folha de ofertas e recapturar `first-fold-measurements`. |
| 5 | `assets/editorial.css` | `.state__paths a` (grade `minmax(0,1fr) auto`) | `404.html`, `obrigado*.html` | Funciona quando o texto e o `small` ficam dentro de um `<span>` (como feito no lote). Se outro lote escrever `<a>texto<small>…</small><svg/></a>` sem o `span`, o `small` cai na segunda coluna. Vale documentar o padrão no caderno ou tornar a regra tolerante (`.state__paths a>small{grid-column:1}`). |
| 6 | `docs/campaigns/design-institucional/expansao/tools/capture.mjs` | registro `320x700` no manifesto | evidência | Cada recaptura acrescenta um registro de overflow 320 sem deduplicar (as rotas capturadas mais de uma vez aparecem repetidas no `manifest-lote-a.json`). Filtrar por rota como já é feito para 390/1440. |
| 7 | `scripts/site/test_inbound_gates.py` / `medicoes-glosas-obras-publicas/index.html` | `test_measurement_delay_canary_389_is_single_url_and_fail_closed` | pilar de medições (integrador) | Falha já no commit base do lote (8860f5577), antes de qualquer alteração deste ramo; não é do lote A. |
| 8 | `scripts/site/test_contact_journeys.mjs` | `journey_harness_runtime` (transição de etapa do formulário da home) | home (integrador) | Falha igual no clone limpo do commit base e no clone do lote (`home form step transition did not activate`, `next_hit_target:false` no Chromium headless local). As 154 verificações das rotas do lote A passam; a falha está fora do lote. |
