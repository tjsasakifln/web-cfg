# Pedidos do lote A ao integrador

Todas as rotas foram fechadas com o CSS existente. **Um pedido bloqueia o `site-ci` do ramo de
integração até o integrador agir: o 3 (baseline de uso de CSS).** Os demais são melhorias, limpezas
ou registros de falhas herdadas do commit base (8860f5577), fora do lote A.

| # | Arquivo do integrador | Seletor / trecho | Página(s) | Motivo |
| --- | --- | --- | --- | --- |
| 1 | `quantitativos-orcamento-obras/index.html` | `<!-- plate:recorte-banheiroNone -->` | `/quantitativos-orcamento-obras/` | O marcador do slot não casa com a regex de `scripts/demonstrative/plates/inline.py` (`<!-- plate:([a-z0-9-]+)( eager)? -->`), então `inline --check` passa vazio para essa rota e a prancha não é re-materializada se o SVG mudar. Corrigir para `<!-- plate:recorte-banheiro -->` (o `<picture>` já materializado é o correto). |
| 2 | `scripts/demonstrative/plates/build_manifest.py` | `PLATE_META` | manifesto de ativos | Roteiro fixo com P1–P4. As oito pranchas do lote (`interferencia-verga-viga`, `revisao-conferencia`, `drenagem-rede`, `inspecao-fachada`, `pericia-fachada`, `sst-canteiro`, `pacote-entrega`, `interfaces-versoes`, códigos PA–PH) estão em `assets-lote-a.json` com origem, licença e hash; acrescentar ao manifesto ou derivar `PLATE_META` de `render_plates.PLATE_SOURCES`. Os códigos usam letras porque o gate de numerais (`test_visible_numbers_come_from_the_sources`) rejeita "P5"…"P10". |
| 3 | `data/design/css-usage-baseline.json` (ou `styles.css`) | `bundles.styles.css.unused`: `.is-dark`, `.proof-figure--pair`, `.sla-box`, `.split--even` | `/compatibilizacao-projetos-engenharia/`, `/projetos-complementares-engenharia/`, `obrigado*.html` | **Bloqueia `site-ci` (`.github/workflows/site-ci.yml:287`, `npm run audit:css-usage`).** A recomposição do lote removeu os últimos usos dessas quatro classes de `styles.css` (no commit base eram usadas só por essas rotas); o gate compara com o baseline, que permite apenas `.opaque-token`. Resultado no HEAD do lote: `FAIL new_unused_class styles.css .is-dark` / `.proof-figure--pair` / `.sla-box` / `.split--even` (+ `FAIL decoration_regression border_radius 162>158`, que já falha no commit base 8860f5577). O lote não reinseriu as classes no HTML para satisfazer o contador (seriam marcação decorativa; `.sla-box` foi retirada de propósito) e não recaptura o baseline porque ele é a última etapa da cadeia de recapturas de CSS (análise → frozen specs → primeira dobra → baselines), só com o CSS congelado. Ação: `python3 scripts/site/audit_css_usage.py --write` na cadeia de fechamento, ou podar as quatro regras em `css/components.css`/`styles.css`. As regras órfãs de `parcerias-engenharia/styles.css` (`.partner-hero*`, `.partner-lead`, `.partner-signal`, `.partner-steps`, `.partner-step`, `.partner-contact*`, `.channel-actions`) já foram podadas pelo lote (folha da rota, fora dos bundles auditados). |
| 4 | `styles-offers.css` | herói em degradê claro (`.offer-hero`/fundo do herói de ofertas) | 11 rotas que carregam a folha: `/comercial/radar-decisorio/`, `/bid-room-licitacoes-obras/`, `/casos/`, `/entregas/`, `/acompanhamento-contratos-obras/`, `/diretoria-b2g/`, `/servicos-obras-publicas/`, `/defesa-tecnica-contratos-publicos/`, `/defesa-margem-contratos-publicos/`, `/diagnostico-b2g-expansao/`, `/atrasos-prorrogacao-obras-publicas/` | O contrato de design proíbe degradê decorativo; `/comercial/radar-decisorio/` ficou PRESERVADA_COM_JUSTIFICATIVA (oferta paga com formulário, preço e medição de primeira dobra). A folha **não** é de rota exata: a troca do fundo afeta as 11 rotas e exige a cadeia de recaptura (hash da análise aprovada → frozen specs → `first-fold-measurements`/`test:first-fold-contract` → baselines), só com o CSS congelado. Fica com o integrador na campanha de fechamento. |
| 5 | `assets/editorial.css` | `.state__paths a` (grade `minmax(0,1fr) auto`) | `404.html`, `obrigado*.html` | Funciona quando o texto e o `small` ficam dentro de um `<span>` (como feito no lote). Se outro lote escrever `<a>texto<small>…</small><svg/></a>` sem o `span`, o `small` cai na segunda coluna. Vale documentar o padrão no caderno ou tornar a regra tolerante (`.state__paths a>small{grid-column:1}`). |
| 6 | `docs/campaigns/design-institucional/expansao/tools/capture.mjs` | registro `320x700` no manifesto; passo do menu | evidência | (a) Cada recaptura acrescenta um registro de overflow 320 sem deduplicar (as rotas capturadas mais de uma vez aparecem repetidas no `manifest-lote-a.json`). Filtrar por rota como já é feito para 390/1440. (b) O passo do menu clica em `.menu-toggle` sem conferir se está visível; numa página `no-js` (404.html, `/privacidade/`, `/termos-de-uso/`, onde `.no-js .menu-toggle{display:none}` e a navegação móvel é estática) a ferramenta aborta com `Node is either not clickable`. Para capturar `/404.html` o lote rodou uma cópia temporária da ferramenta com a única diferença `if (toggle && toggleVisible)` (diff registrado no relatório); a cópia não foi commitada. Tornar o clique condicional. |
| 7 | `medicoes-glosas-obras-publicas/index.html`, `data/bofu-dominance/frozen-specs/**`, `scripts/commercial/render_contract_defense_products.mjs:62`, `scripts/site/test_inbound_gates.py:278` | quatro suítes: `npm run test:inbound-gates` (`FAIL test_measurement_delay_canary_389_is_single_url_and_fail_closed medicoes-glosas-obras-publicas/index.html`), `npm run test:page-contract-contratos` (`CONTRACT_DEFENSE_FROZEN_DRIFT: medicoes-glosas-obras-publicas/index.html expected=124e31f2… actual=7288c195…`), `npm run test:bofu-dominance` (`9 failed, 108 passed`: `test_drift_policy_separates_html_rendering_and_recapture`, `test_prepare_only_state_keeps_every_forbidden_surface_at_its_committed_hash`, `test_git_diff_is_exclusive_area`, …), `npm run test:first-fold-contract` (`first-fold-contract: 3 check(s) failed`, `evidence_/medicoes-glosas-obras-publicas/_html_bytes_match`) | pilar de medições (integrador) | O pilar mudou no piloto (`git diff 47da03b64 8860f5577 -- medicoes-glosas-obras-publicas/ data/bofu-dominance/frozen-specs`: 5 arquivos, 212 inserções) sem recaptura dos hashes congelados. As quatro suítes reprovam **identicamente** no commit base 8860f5577 (reproduzido em worktree isolada do base) e no HEAD do lote, e passam em `origin/main` 47da03b64. `git diff 8860f5577..HEAD --name-only -- medicoes-glosas-obras-publicas/ tests/bofu_dominance/ data/bofu-dominance/ scripts/commercial/ tests/commercial/` é vazio: o lote A não toca nesses arquivos. Recaptura honesta com motivo via `scripts/bofu_dominance/frozen_specs/materialize.py` + `first-fold-measurements`, na cadeia de fechamento do integrador; até lá o ramo de integração reprova essas suítes. |
| 8 | `scripts/site/test_contact_journeys.mjs` | `journey_harness_runtime` (transição de etapa do formulário da home) | home (integrador) | Falha igual no clone limpo do commit base e no clone do lote (`home form step transition did not activate`, `next_hit_target:false` no Chromium headless local). As 154 verificações das rotas do lote A passam; a falha está fora do lote. |
| 9 | `css/components.css` (ou `assets/editorial.css`) | (a) tabelas de 3 colunas em `.table-scroll` abaixo de 620 px (`.data-table--stack`: uma linha por `tr`, `th[scope=row]` como rótulo); (b) `.phases+.plate,.list-ruled+.plate,.steps+.plate{margin-top:clamp(1.5rem,3vw,2.5rem)}`; (c) `.data-table--keyed th:first-child{width:18%}` | `/assistencia-tecnica-pericial-engenharia/` (evidência), `/projetos-complementares-engenharia/` (matriz), `/seguranca-trabalho-apoio-tecnico/` (diagnóstico, 4 col.) | Achados BAIXA/PREFERENCIA da revisão que pedem CSS compartilhado: no 390 as tabelas de 3 colunas rolam e escondem a coluna-chave na primeira leitura; a prancha PH depois das fases usa hoje a combinação `plate plate--dominant proof-figure` só para herdar `.phases+.proof-figure{margin-top}` (trocar pela regra (b) e retirar a classe extra); a primeira coluna da tabela de evidência tem ~400 px para rótulos curtos. Rolagem continua correta para memória/orçamento. |
| 10 | `docs/campaigns/design-institucional/expansao/caderno-lotes.md` §2 | pranchas em rotas com contrato de `src` fixo | `/projetos-complementares-engenharia/` | Registro de padrão: quando um teste de contrato ou um kit exige um `src` fora de `assets/pranchas/` (aqui `assets/projetos-complementares-engenharia/{pacote-entrega,interfaces-versoes}.svg`, exigidos por `tests/inb-20260911/12` e por `data/distribution/partner-reference-kits.v1.json`), o lote gera a prancha normalmente, copia a composição desktop para o caminho legado e escreve a `<picture>` à mão (`<source media="(max-width:699px)">` móvel + `<img src=legado>`); o teste da família prova a igualdade byte a byte. O slot `<!-- plate:… -->` não cobre esse caso; se o integrador preferir, `inline.py` pode aceitar um atributo de destino. |

## Onda 2 (`/entregas/`)

| # | Arquivo do integrador | Seletor / trecho | Página(s) | Motivo |
| --- | --- | --- | --- | --- |
| 11 | `assets/editorial.css` → subconjunto para hubs gerado por `scripts/site/build_css.py` (como `editorial-article.css` e `editorial-tool.css`; `test:design` roda `build_css.py --check`) e recaptura de Lighthouse sobre `_site` | tipos (`.t-*`), `.sec*`, `.sec-head*`, `.page-index*`, `.svc-open*`, `.conditions*`, `.after*`, `.contact-primary/.contact-alt`, `.nowrap` | `/entregas/` (rota crítica do gate de Lighthouse; serve também `/servicos/`, `/casos/`) | O hub usa ~9 KB brutos da folha editorial de 42 KB (7,7 KB gzip). Medido (runner do repositório, 3 runs, árvore fonte com gzip, mesma máquina): antes 144.989 B / LCP 1.803–1.805 ms / FCP 1.354–1.358; depois 148.743 B / LCP 1.953–1.957 / FCP 1.504–1.510 (`MEASURED_PASS`, 45 ms do teto). Variantes com Lighthouse direto (tabela no relatório): o subconjunto de 9,4 KB devolve o **FCP** a 1.366 ms (−145 ms) e o eixo largo do h1 responde pelos mesmos 145 ms; o **LCP** é invariante (~1.960 ms em dez variantes, inclusive sem folha editorial e no HTML antigo com ela), porque o elemento LCP passou a ser o `h1` e sua pintura simulada cai nesse ponto. Não há alavanca de LCP no lote (`build_css.py` e `editorial.css` são do integrador). Ação: gerar o subconjunto para hubs (recupera o FCP) e medir o LCP de `/entregas/` sobre `_site` no CI antes da promoção; a nota `aceite-runtime-lighthouse-borda` registra ~300 ms de diferença laboratório→borda e três reversões por isso. **Após a revisão** (`--label=onda2-entregas-fix`): LCP 1.953–1.967 ms, FCP 1.356–1.504, DOM 854, conteúdo 150.487 B (teto 153.600; a prancha PG móvel, 1.255 B gzip, entra na carga medida mesmo `lazy`). Se a borda medir > 2.000 ms, a alavanca de rota que sobra é reverter o eixo largo do `h1` nesta rota (−145 ms de FCP demonstrados); a prancha pode ser retirada da rota se o teto de conteúdo apertar. |
| 12 | `data/commercial/first-fold-measurements.v1.json` | `routes[route=/entregas/]` (`html_sha256` `a97308da…`, `input_hashes` de `entregas/index.html` e `entregas/styles.css`) | `/entregas/` | Recaptura de primeira dobra na cadeia de fechamento: os cinco papéis continuam nos seletores das regras (`header .eyebrow` bare, `h1`, `.deliverables-lead`, `.offer-proof-line`, uma `a.button-primary`), ação dominante a 669 px (390×844) e 670 px (1366×768) medidos no Chromium. |
| 13 | `scripts/pseo/build_site.py` / `docs/editorial/contract-analysis/review/13ec…/` (`approvals.json`) | `FAIL-CLOSED contract-analysis build: contract_analysis_build_missing` (`approval_rendered_hash_mismatch`) | `npm run build:site` | Reprova identicamente na base `2da310422` (worktree isolada) e no HEAD do lote; é a etapa "approvals.json antes do build" da cadeia de fechamento de CSS. Enquanto isso, `test:responsive-matrix` e `test:html-integrity:site` não têm `_site` para rodar e `test:deliverables-hub-ui`/`test:ui`/Lighthouse foram rodados sobre a árvore fonte. |
| 14 | `scripts/site/audit_deliverables_live.mjs` (`audit:deliverables-live`, L419–421) | `capability_count`, `capability_semantics`, `capability_identity_or_state_drift` leem `[data-capability-id][data-public-state]` e esperam 54 linhas | `/entregas/` | Contradição pré-existente (não é da onda 2): `tests/commercial/test_task_doors.mjs::public_page_has_no_maturity_census` **proíbe** `data-capability-id` na página desde 2026-09-09, e o HTML não emite essas linhas desde então. `site-ci.yml:245` roda só `test:deliverables-live-audit` (contrato, passa); quem rodar o audit ao vivo depois da promoção verá essas três falhas independentemente desta onda. Alinhar o audit ao contrato atual (8 ofertas, sem censo interno). |

## Onda 2 — após a revisão (`/entregas/`)

| # | Arquivo / dono | Seletor / trecho | Página(s) | Motivo |
| --- | --- | --- | --- | --- |
| 17 | Integrador / fundador (decisão de conversão) | `nav.offer-decision-nav.page-index` dentro de `section#enquadrar`, logo após `header.sec-head`; entrada `02 Análises com preço publicado` do `nav.page-index` da abertura | `/entregas/` | A revisão (R2) apontou que o índice pela decisão (entrada das oito ofertas pagas) saiu de ~1.017 px para ~4.361 px a 390 na onda 2 e que o guarda do gate tinha sido afrouxado sem decisão registrada. Decisão do lote, para confirmação: o índice é o primeiro bloco da seção das ofertas e a entrada 02 do índice da página (a 1.439 px a 390, dentro da abertura) leva até ele em um toque; restaurar a emissão antecipada colocaria dois índices em sequência na abertura (a caixa navy que a campanha retira). O gate `test_deliverables_hub_ui.mjs` passou a medir a posição **relativa** (≤ 500 px do topo de `#enquadrar`; entrada ≤ 1.800 px). Se o fundador preferir o índice antes dos serviços, basta devolver `renderDecisionNav` a `renderCatalog` no gerador e ajustar os dois invariantes. |
| 18 | `data/commercial/page-contract-eight.v1.json` (dono do contrato de dados do catálogo) | `package` (forma de pagamento), `items[].value_first.*`, `objeto_incluido`, `saida_minima`, `sla` | `/entregas/` | (a) "pagamento único" não existe no contrato (o pacote tem `credit_*` mas não forma de pagamento); o lote reintroduziu a condição com a copy literal da base no passo 02 da escada — vale um campo (`package.payment_terms_pt_br`) para o gerador derivar. (b) PREFERÊNCIA da revisão: valores começando em minúscula ("uma empresa, um recorte…", "após informações necessárias confirmadas.", "leitura de direção…", "módulos já reconciliados…") e PRAZO / QUANDO A CONTAGEM COMEÇA repetindo "após informações necessárias confirmadas" nas ofertas 01 e 08. Copy protegida (§3), herdada da base; corrigir no dado, não no lote. |
| 19 | Dono da rota `/entregas/` (família `entregas`) | `ul.contact-alt` do bloco escuro `#captura-entregas` | `/entregas/` | A revisão (BAIXA) notou que a rota não tem `wa.me` enquanto o esqueleto do caderno prevê `.contact-alt` (WhatsApp, e-mail). A base também não tinha; o lote não acrescentou (link de contato novo com texto pré-preenchido é §3; o formulário é a ação dominante e o e-mail a alternativa). Se autorizado, usar o texto já publicado em `/servicos/` ("Olá, Tiago. Quero explicar uma situação técnica e entender o próximo passo.") e regerar o censo de CTAs. |
| 20 | `data/commercial/page-contract-eight.v1.json` ou contrato de copy do hub | item "Serviços de engenharia por proposta." de `section#condicoes-e-limites` (gerador, hard-coded) | `/entregas/` | R2 (BAIXA): frase de condição fora de contrato de dados. Mantida: deriva do AGENTS.md (confirmar escopo, local, campo, atribuição profissional e ART antes do aceite técnico) e o `aside-note` "Antes do aceite técnico" da abertura remete a ela. Registrar como copy autorizada do hub ou derivar de `common_boundaries`. |
| 21 | `scripts/site/shell_nav.py` | `nav#mobile-menu`: botão "Fechar" dentro do painel + X no cabeçalho | todas (casca) | PREFERÊNCIA da revisão: dois controles de fechar no menu móvel (`entregas-390x844-menu-onda2.jpg`). Casca do integrador. |
| 22 | Revisor R2 / integrador | último achado (BAIXA) da revisão de R2, truncado na entrega ("ramo campaign/salto-02/lote-a (HEAD de1984156, merge-base 2da3104…") | — | Sem texto para agir; recuperar da revisão original antes do fechamento. |

## Correção do aceite (síntese de 2026-09-17, HEAD de integração a7068f671)

Achados da síntese que caem em arquivo do integrador ou em decisão do proprietário. Cada linha traz o diff
sugerido; o lote A não tocou nesses arquivos.

| # | Achado | Arquivo do integrador | Diff sugerido | Motivo |
| --- | --- | --- | --- | --- |
| 23 | **B-36** (veracidade) | `index.html` linha 248 (`figcaption#delivery-3-cap`) | `<span class="t-kicker">Estrutura da análise · Avaliação de imóvel</span>` → `<span class="t-kicker">Exemplo demonstrativo · Avaliação de imóvel</span>` | As três outras legendas da série abrem com o rótulo; o contrato exige o rótulo "na folha e na legenda". Recaptura da primeira dobra depois. |
| 24 | **B-28** | `index.html` linhas 132, 141, 182 (`p.area__sub[aria-label]`) e 336 (`div#mercado-pncp[aria-labelledby]`) | `<p class="area__sub" aria-label="…">` → `<nav class="area__sub" aria-label="…">` (mesmo conteúdo, ou `<p>` sem `aria-label` com um `<span class="visually-hidden">` se a folha ganhar a utilidade); `<div aria-labelledby="market-title" … id="mercado-pncp">` → acrescentar `role="region"` | `aria-label`/`aria-labelledby` em elemento genérico é ignorado (axe `aria-prohibited-attr`, 4 nós). |
| 25 | **B-34** | `js/modules/form.js` (bloco `situationChannelsEl`, ~linha 83) + `index.html` linha 530 | Dar ao `<a data-cta-id="home-situation-email">` um `data-situation-email` e, no runtime, `situationEmailEl.setAttribute('href', 'mailto:tiago.sasaki@confenge.com.br?subject=' + encodeURIComponent(fit.email_subject \|\| fit.route_label) + '&body=' + encodeURIComponent(fit.whatsapp))`; para a opção "Problema urgente em contrato" (sem `fit.route`) manter o parágrafo visível com os canais genéricos em vez de `hidden`. | O WhatsApp recebe texto contextual, o e-mail fica sem assunto/corpo e o telefone sem contexto; na opção 8 o parágrafo some. |
| 26 | **B-10** | `index.html` linha 536 (`div.cf-turnstile`) + `js/modules/form.js` (validação da etapa 2) | `<div class="cf-turnstile" data-theme="light" data-size="normal" data-language="pt-br"></div>`; na validação nativa excluir os controles da etapa oculta (`form.querySelectorAll('[hidden] [required]')` → `required=false` enquanto a etapa está oculta, ou `reportValidity()` só no fieldset visível) | Widget em inglês em página pt-BR; console `An invalid form control with name=consentimento is not focusable`. |
| 27 | **M-04** (parte do integrador) | `quantitativos-orcamento-obras/index.html` linha 282 | `<div class="table-scroll" tabindex="0">` → `<div class="table-scroll" role="region" tabindex="0" aria-labelledby="<id do h3 da planilha>">` e `<table class="data-table data-table--wide" aria-labelledby="<id da figcaption>">` (padrão aplicado em `/projetos-complementares-engenharia/`, `/assistencia-tecnica-pericial-engenharia/` e `/seguranca-trabalho-apoio-tecnico/`) | Contêiner rolável focável sem nome; tabela sem nome acessível. |
| 28 | **M-06** (parte do integrador) | `quantitativos-orcamento-obras/index.html` (`section#triagem-quantitativos > .capture-grid`) | Mesma reordenação aplicada nas sete rotas do lote: na primeira coluna ficam `t-kicker`, `h2`, `h3` dos canais e `div.contact-primary`; a segunda coluna vira `<aside aria-label="O que informar e o que acontece depois">` com a prosa (`p[data-form-value]`, lista "Ajuda a informar", parágrafo), `ol.after` e os `p.contact-note`. Medido no lote: ação dominante a 282–391 px do topo após o salto (antes 609–1036 a 390×844; quantitativos mede 1.037). | O CTA verde da abertura rolava até o título e a ação real ficava abaixo da dobra no celular. |
| 29 | **P-06** | `triagem-tecnica/index.html` linha 70 | `>Ligar para explicar a demanda: (48) 98834-4559</a>` → `><span>Ligar para explicar a demanda: <span class="nowrap">(48) 98834-4559</span></span></a>` (`.nowrap` já existe em `assets/editorial.css:173`; o `<span>` externo é necessário porque o `<a>` é `inline-flex` e um único filho mantém o fluxo de texto — aplicado nos `contact-alt` das sete rotas do lote) | Telefone quebrando entre linhas a 390. |
| 30 | **B-01** | `scripts/demonstrative/private_project/render.py` (plantas PR-ARQ R00/R01) | Deslocar a cota vertical "1,80 m" para fora do rótulo "HS-01" (ou o rótulo para o outro lado do poço) e gerar variante móvel reenquadrada, como já feito para o caso de infraestrutura | Rótulo sobre a cota nas duas plantas; composição desktop escalada a 351 px a 390. |
| 31 | **B-06** (parte da ferramenta) | `docs/campaigns/design-institucional/expansao/tools/capture.mjs` | Ver pedido 6(b); com `script.js` no 404 a ferramenta voltou a capturar a rota sem cópia local, mas o guarda continua necessário para `/privacidade/` e `/termos-de-uso/` | `Node is either not clickable` em páginas `no-js`. |
| 32 | **_headers** (consequência do P-04) | `_headers` (gerado por `npm run csp:refresh` dentro do `build:site`) | Nenhuma edição manual: ao remover os doze `style="min-height:44px;min-width:44px"` de `parcerias-engenharia/index.html`, o hash `sha256` desse valor deixa de ser necessário e sai sozinho no próximo `csp:refresh` | Parcerias era a única rota com esse valor inline. |
