== ad8006990b04557e9 FIX_REQUIRED
 - P1 netlify/functions/lib/lead-core.cjs 552 | isSearchEngineHost matches the engine label anywhere in the host (labels.indexOf), so any Google/Yahoo product subdomain reads as organic search. Verified with node: mail.google.com, docs.google.com, drive.google.com, accounts.google.com, translate.google.com, groups.google.com, sites.google.com and mail.yahoo.com all return search_organic. A click from a Gmail message (including a cold outbound e 
    FIX: Require the engine label to be the first label after stripping www., or allow only an explicit prefix allowlist before it (search., images., news., 2-letter cctld such as br.) instead of any subdomain; add negative cases mail.google.com, docs.google.com, drive.google.com, mail.yahoo.com -> referral 
 - P1 scripts/revops/proposal_counting.mjs 380 | Undocumented counting rule: an accepted observation without amount_cents silently takes the emitted fee_total_cents. No rule in data/revops/proposal-counting.v1.json (PC-07, observation_schema.amount_cents = 'valor do estágio') authorizes this fallback, yet fixtures a-organic-known and g-annual-proposal depend on it (their accepted events carry no amount_cents and expect by_stage.accepted = fee_to 
    FIX: Either add the fallback explicitly to PC-07 (accepted sem amount_cents assume fee_total_cents da emissão contada) and to observation_schema.fields.amount_cents, or remove the fallback and exclude with the existing stage_without_amount reason, updating fixtures a and g (add amount_cents or expected e
 - P2 scripts/revops/test_proposal_counting.mjs 82 | Reconciliation checks read closedLoopSrc[462] and closedLoopSrc[514] by absolute line index. Any insertion above line 463 of netlify/functions/lib/closed-loop.cjs (unrelated work) fails npm run test:revops with 'reconciliation_line_463_drifted'. 
    FIX: Anchor on a whole-file regex (/duplicate_entity.*opportunity_already_has_proposal/ and /contract_value = .*: revenue/) and, if a line reference is desired, compute it and print it instead of asserting the index; relax the ':463'/':515' citations in proposal-counting.v1.json and closed-loop-funnel.v1
 - P2 data/revops/proposal-counting.v1.json 167 | storage_note_pt_br says the web class is 'Gravada na função de lead', but as merged the value is derived in validateAndNormalize and then discarded: buildLeadRecord (netlify/functions/lib/lead-store.cjs) and toExportRecord (scripts/revops/export_leads.mjs) enumerate columns and do not copy origin_class (confirmed by the test's own 'not_yet_in_store_record' outcome). Staying out of those files resp 
    FIX: Reword to 'Derivada na função de lead; ainda não persistida na linha do store' and list the two follow-up lines (lead-store.cjs buildLeadRecord, export_leads.mjs toExportRecord) as integrator tasks in the campaign README or issue #706.
 - P2 data/revops/proposal-counting.v1.json 161 | web_origin_class.derivation.campaign counts only utm_source/utm_medium; a link carrying only utm_campaign (which is in ATTR_ALLOWLIST) is classed direct_or_unknown (verified: deriveOriginClass({utm_campaign:'x'}) -> direct_or_unknown). Code matches the contract text, but a class named 'campaign' that misses a campaign-tagged arrival is a contract gap. 
    FIX: Decide explicitly: either include utm_campaign (and utm_content/utm_term) in the 'campaign' condition in lead-core.cjs deriveOriginClass and the contract/doc table, or state in the contract why only source/medium count.
 - P2 netlify/functions/lib/lead-core.cjs 563 | host === 'localhost' || host === '127.0.0.1' is dead code: both hostnames are already in ownHosts() via ALLOWED_ORIGINS (http://localhost:8765/8766, http://127.0.0.1:8765/8766). 
    FIX: Drop the two literal comparisons or keep them with a comment that they guard against ALLOWED_ORIGINS edits.
 - P2 scripts/revops/proposal_counting.mjs 374 | by_stage is not filtered by options.close_month: with a September window, invoiced/received events dated October or November still accumulate into by_stage for September-emitted proposals, while the contract's close section says stages 'fecham no mês do próprio evento, em leitura separada'. Also origin_evidence is not checked against the contract's 'nunca URL completa' rule (PII_KEYS covers url/re 
    FIX: When options.close_month is set, apply closeMonth(obs.at) to stage events as well (or document that by_stage is unwindowed); add a guard that origin_evidence does not match /^https?:\/\//.
 - P2 scripts/revops/test_lead_stages.mjs 308 | Working-tree diff on this file is not W7's: it is outside W7's declared file list and its own comment identifies W6-ops-web (e-mail provider handles in publicLeadSummary). Verified and excluded from this review; reported so it is not attributed to W7. 
    FIX: No action for W7; W6 reviewer owns it.
== a3a6bb2be7f6e5533 FIX_REQUIRED. What holds: both JSON files parse; all 19 `regra_citada` strings re-verified byte-for-byte (whitespace-normalised, bracketed redactions split) against the three rules pages re-downloaded on 2026-09-19 (HTTP 200 each); secondary claims (RG/CPF/endereço on the IBEC form, 12 laudas/1200 toques, MS Word, 300 dpi, 15 dias de 'de acordo', telefone 5575-8155, 'foto do autor' optional in B / requested in C) all confirmed on the pages; the asset description (três itens glosados, mérito vs formalidade, quantidade declarada/medida/evidenciada + critério contratual, vistoria conjunta / reapresentação instruída / parcela incontroversa, diário de obra / relatório fotográfico / levantamento, JSON-LD disambiguatingDescription 'contexto, números e órgãos são hipotéticos') matches casos/medicao-glosa-demonstrativo/index.html; 'exemplo demonstrativo' appears once per mensagem_util; no AI/jargon/price/deadline/credential/norm claims in authored copy (only hits: branch/path name and pre-existing W4 line 115); mensagem_util is pt-BR, integral, keeps órgão-decide / não-perícia / sem promessa. Gates re-run: python3 scripts/site/test_ai_mention_gate.py -> PASS (231 files, rc=0); python3 scripts/site/test_integral_solution_copy.py -> PASS (rc=0); python3 scripts/site/html_integrity.py --root . --surface source -> failures=0 (233 files). NOTE: these gates scan public HTML only and validate nothing in either JSON; W8 disclosed this correctly. Why FIX_REQUIRED: W8's restructure of distribuicao-preparada.json left the campaign README's authorization step (README:112, :116, :191) mis-routed (block now has 2 entries, README says three, named channels / IBEC rights cession + RG/CPF / B-C originality conflict never surfaced to the authorizer); the retained EXT-3 rule 'entidade específica não nomeada até a decisão de contato' is contradicted 8 lines below by named entities with no recorded authorization for lifting that restraint, in a PUBLIC repository; CANAL-A has no proposal step (the IBEC form takes the finished work), so the file's stated deliverable 'mensagem_util pronta' is not actionable there and the fact that no article exists yet (and who its natural-person author with civil/penal responsibility would be) is not recorded as a state. Nothing was sent or posted; no boundary of AGENTS.md (warmbly/extra-cli/governance) is crossed. Items silently skipped: none beyond what W8 listed in left_undone; README update was correctly flagged as outside its ownership but is blocking, not optional.
 - P1 docs/campaigns/inbound-receita-20260919/README.md 112 | After W8 moved EXT-1 to `conexoes_internas_candidatas`, the block `oportunidades_externas_preparadas` has two entries, but README:112 still says 'Até três oportunidades', lists item 1 as external (/conteudos/), routes the authorizer to that block only, and README:191 repeats 'três oportunidades em evidence/distribuicao-preparada.json'. The founder/Governança step therefore never sees the three nam 
    FIX: Integrator (README owner): rewrite README §Distribuição items 1-3 and line 191 to say EXT-1 is an internal connection, point the authorization step at `canais_externos_verificados` and at docs/campaigns/bofu-integral-20260919/evidence/canais-externos-verificados.json, and list the three material con
 - P1 docs/campaigns/inbound-receita-20260919/evidence/distribuicao-preparada.json 63 | EXT-3 retains verbatim 'entidade específica não nomeada até a decisão de contato' (and README:112/:116 state 'sem nomes de pessoas/organizações privadas' as a property of the prepared state), yet the new block at line 71 names IBEC, Instituto de Engenharia and Revista Brasil Engenharia as prospecting targets with drafted pitches, before any contact decision, in a repository whose visibility is PUB 
    FIX: Add one field to `canais_externos_verificados` (e.g. `nomeacao_de_canais`: 'autorizada pelo mandato W8-distribuicao-docs de 2026-09-19 (#707); nomes são de veículos com página pública de submissão, não destinatários pessoais; repositório é público') and have the integrator update README:116 accordin
 - P1 docs/campaigns/bofu-integral-20260919/evidence/canais-externos-verificados.json 52 | mensagem_util for CANAL-A is written as a 'Proposta de artigo' to be sent, but the IBEC channel (formato_aceito, line 43) has no proposal step: the form requires the finished file plus the author's RG/CPF/endereço and acceptance of the cession terms. The article does not exist yet in any of the three channels, all three mensagem_util describe it in the present tense ('O artigo mostra…', 'O texto d 
    FIX: Add to each channel `artigo_redigido: false` and `autor_responsavel: 'a definir por Governança/fundador (pessoa física com atribuição profissional comprovada; assume responsabilidade civil e penal no IBEC)'`; for CANAL-A rename/annotate mensagem_util as `resumo_do_artigo_a_redigir` (no cover message
 - P2 docs/campaigns/bofu-integral-20260919/evidence/canais-externos-verificados.json 107 | Seven verbatim excerpts of brasilengenharia.com's rules page are reproduced in a PUBLIC repository, including the page's own notice 'É proibida a reprodução do conteúdo desta página … sem autorização escrita da Editora'. Short citation with source is lawful (Lei 9.610/98 art. 46 III), but the volume quoted (most of items 1-9) exceeds what is needed to evidence the rule, and the same applies to CAN 
    FIX: Keep at most 2-3 short quotes per channel (originalidade; 'A aprovação de um artigo não implica na sua publicação'; envio), paraphrase the rest in `condicoes_materiais_do_canal`, and record `sha256_html_consultado` of the downloaded page as the verification evidence instead of full quotation.
 - P2 docs/campaigns/bofu-integral-20260919/evidence/canais-externos-verificados.json 76 | formato_aceito for CANAL-B (line 76) and CANAL-C (line 109) says 'artigo técnico inédito'; neither page uses the word 'inédito' and rule 7 admits enumerated exceptions (interest in wider divulgação; thesis; congress; international). The compression overstates a rule the file quotes correctly in regra_citada, and the founder may read it as an absolute bar to re-using the published demonstrative. 
    FIX: Replace 'inédito' with 'não publicado em outro veículo, salvo exceções a juízo da Comissão Editorial (regra 7)' in both formato_aceito strings; keep the operational advice that the published case is a reference, not the article.
 - P2 docs/campaigns/inbound-receita-20260919/evidence/distribuicao-preparada.json 5 | Authorization owner is inconsistent inside one file: estado_geral (line 5) and the new block say 'Governança/fundador'; EXT-2/EXT-3 autorizacao (lines 49, 63) and the retained EXT-1 say 'owner de aquisição'. README:5 (mandate) reserves publicação externa/prospecção to the founder. Kept as 'history' is defensible, but an authorizer reading only the entry gets a different owner. 
    FIX: Add to EXT-2/EXT-3 `autorizacao_atual: 'PENDENTE: Governança/fundador (ver canais_externos_verificados)'` next to the retained historical string, so history and current owner coexist explicitly.
 - P2 docs/campaigns/bofu-integral-20260919/evidence/canais-externos-verificados.json 178 | The evidence for #707 now lives in docs/campaigns/bofu-integral-20260919/ while docs/campaigns/inbound-receita-20260919/README.md:3 declares itself 'a única trilha canônica' for #705-#707 and docs/campaigns/bofu-integral-20260919/README.md does not mention W8, channels or distribution. The path was mandated by the task, but neither README acknowledges the split. 
    FIX: Integrator: add one line to docs/campaigns/bofu-integral-20260919/README.md naming the W8 evidence file and one back-pointer in the inbound README §Distribuição (covered by the P1 README fix).
== a39d53a7751fce696 FIX_REQUIRED
 - P1 assistencia-tecnica-pericial-engenharia/index.html 197 | New step 01 'Triagem técnica pré-litígio' renders a separate paid catalog unit (pre_litigation_technical_screening, paid_triage_rule 'Triagem técnica é unidade paga') as a mandatory step of the contracted assistance under the header 'Quatro passos, sempre na mesma ordem' (L194). The li text itself says 'Antes de contratar a assistência completa', i.e. something that happens before contracting, con 
    FIX: Reorder and qualify: keep 'Delimitar objeto e papel' (with the conflict check) as step 01; make the triagem an optional movement that follows it, e.g. '<h3>Triagem técnica pré-litígio, quando a proposta a inclui</h3><p>Se ainda não se sabe se há matéria técnica de engenharia na discussão, a proposta
 - P1 assistencia-tecnica-pericial-engenharia/index.html 105 | Lead paragraph now opens with a dangling fragment 'Original e sintético: duas partes divergem...'. The removed words ('Exemplo demonstrativo') were the noun the adjectives agreed with; the result is not idiomatic pt-BR. The duplication being solved was between a header paragraph and the figcaption tag (two different blocks), so the removal was not required by the once-per-figure/block rule; the fi 
    FIX: Restore a noun without repeating the label, e.g. 'Caso original e sintético: duas partes divergem...' (or 'Cenário original e sintético:'). Same fix on seguranca-trabalho-apoio-tecnico/index.html L106.
 - P1 seguranca-trabalho-apoio-tecnico/index.html 106 | Same dangling fragment: 'Original e sintético: a planta de um canteiro recebe...' after removing 'Exemplo demonstrativo,' from the lead. Not idiomatic pt-BR; the removal was not required (label lives in figcaption, a different block). 
    FIX: 'Caso original e sintético: a planta de um canteiro recebe...' (keeps once-per-block rule; figcaption tag is the single label).
 - P2 seguranca-trabalho-apoio-tecnico/index.html 198 | Item 03 h3 'Crítica fundamentada de laudo pericial e manifestação técnica' and body 'a manifestação técnica sai com método e conclusão delimitada' add a deliverable ('manifestação técnica') that data/offers/multivertical/catalog.v2.json does not register for labor_sst_technical_assistance (deliverables: quesitos e pontos técnicos; análise de PGR/LTCAT/PPRA histórico/AET/laudos de terceiro; crítica 
    FIX: Rename h3 to 'Crítica fundamentada de laudo pericial e acompanhamento da diligência' and replace 'a manifestação técnica sai com método e conclusão delimitada' by 'o que a engenharia de SST pode afirmar sai delimitado, com método e conclusão'; in 'Serve para' replace 'A manifestação instrui a parte 
 - P2 assistencia-tecnica-pericial-engenharia/index.html 7 | Meta description and JSON-LD WebPage.description were rewritten on both pages although the task did not ask for it (scope creep). Content is truthful and length is fine (155/146 chars), and og:description was left independent, but seo/visible-parity.json still records the previous descriptions for both routes. That file is generated from _site by `npm run parity:site` (a tracked build output), so  
    FIX: Either accept and have the integrator run build:site + parity:site (tracked outputs) before commit, or revert the two <meta name=description> and the matching JSON-LD 'description' strings to the HEAD text to keep the workstream inside its assigned scope.
 - P2 seguranca-trabalho-apoio-tecnico/index.html 193 | 'Empregador, empregado com advogado ou o advogado da parte recebe a leitura técnica...' — the qualifier 'com advogado' can read as an entry condition for the employee (catalog ICP says 'empregado parte com patrono', and the block does say the advogado conduz a estratégia, so it is defensible). Pt-BR otherwise good; roles (perito do juízo, médico do trabalho, advogado, assistente da parte), documen 
    FIX: Optional: 'Empregador, empregado ou o advogado da parte recebe a leitura técnica...; a estratégia processual permanece com o advogado' to avoid reading as an exclusion at the door.
 - P2 assistencia-tecnica-pericial-engenharia/index.html 0 | Verification record. Re-run by reviewer, all from repo root: python3 scripts/site/test_ai_mention_gate.py -> PASS (231 files); python3 scripts/site/test_integral_solution_copy.py -> PASS; python3 scripts/site/test_html_integrity.py && python3 scripts/site/html_integrity.py --root . --surface source -> HTML_INTEGRITY_TESTS_OK, html_files=233 failures=0; npm run test:copy:language -> PASS (0 finding 
    FIX: No action; record only.
== a1e9d0ca0f6d072fd FIX_REQUIRED
 - P0 data/organic/public-family-registry.json 1222 | W1's one-line visitor_job edit changes the sha256 of the registry (HEAD=1a566bd9… → aab2f32e…), and data/bofu-dominance/core/buyer-decision-map.v1.json pins the old hash as a derived_from source contract. Reproduced: `python3 -m scripts.bofu_dominance.core.buyer_decision_map --check` exits 1 with reason `source_contract_hash_drift`; `python3 scripts/site/inbound_gates.py --out <scratch>` reports o 
    FIX: Integrator (file not in W1 ownership): run `npm run bofu-ownership:report` (python3 -m scripts.bofu_dominance.core.buyer_decision_map --write) to recapture the hash in data/bofu-dominance/core/buyer-decision-map.v1.json AFTER all registry edits by other workstreams are final, then re-run `npm run bo
 - P1 inspecao-diagnostico-edificacoes/index.html 159 | Inside the block titled 'Recebimento ou entrega' W1 wrote 'a ABNT NBR 16747 é a referência de inspeção quando aplicável ao objetivo'. Campaign README Brief 2 records the #705 correction that NBR 16747 (inspeção predial) was wrongly treated as if it served recebimento/nexo causal and that the correction was pending in public copy. Net effect of W1's choices (render 16747 in the recebimento block; d 
    FIX: Editorial owner decides: (a) delete the 16747 clause from the Situação 03 paragraph and keep only the line-173 sentence (inspection context), optionally replacing it with 'A vistoria de recebimento não é inspeção predial nem investigação de causa; a proposta nomeia qual delas o seu caso pede'; or (b
 - P2 inspecao-diagnostico-edificacoes/index.html 23 | og:description still carries the old wording ('Registrar e diagnosticar fissura, infiltração ou condição do imóvel…') while meta description (line 7) and JSON-LD WebPage.description were both rewritten to the new deliverable-first text; two of three descriptions updated, hero kicker now names reforma/documentação but the OG card does not. 
    FIX: Set og:description to the same 157-char text used in meta description, matching the convention W1 itself applied on triagem-tecnica (og:description mirrors meta description).
 - P2 inspecao-diagnostico-edificacoes/index.html 160 | Situação 04 (reforma em condomínio) renders the ART-of-third-party limit and the visit condition, but omits the catalog's other material limit for renovation_plan_condo_review: 'aprovação de prefeitura como ato do órgão' / not_promised_for alvará. The as-built block (Situação 05) states this for its own offer; the reforma block does not, so a síndico may read the parecer as clearing municipal appr 
    FIX: Append one clause to the Situação 04 paragraph, e.g. 'A aprovação da prefeitura ou do órgão é ato do órgão e não é prometida aqui.'
 - P2 inspecao-diagnostico-edificacoes/index.html 163 | The disambiguation sentence after ol.phases lists 'Recebimento, inspeção e diagnóstico, investigação de causa (perícia) e plano de reforma' but omits the fifth decision the page now renders (documentação do construído / as-built), so the one-sentence map is incomplete relative to Situação 05. 
    FIX: Add 'e documentação do construído' to the enumeration.
 - P2 triagem-tecnica/index.html 22 | JSON-LD dateModified remains 2026-09-14 although visible copy in li#obra-imovel changed today (W1 self-flagged; no gate enforces it). 
    FIX: Integrator sets dateModified to 2026-09-19 when finalizing the page.
 - P2 projetos-complementares-engenharia/index.html 223 | W1 removed 'Software e modelos são método ou insumo; não ampliam atribuição profissional.' from the method header. Verified the same sentence is still present in step 02 (line 227), so the material limit is retained once; the W1 self-report is accurate. No fix required; recorded so the integrator does not treat the header deletion as a lost limit. 
    FIX: None required.
== ad6facc432da0147f FIX_REQUIRED — W4's editorial copy is clean (pt-BR, no AI mention, no invented claims, material limits preserved in Limite cards and data-form-boundary, hrefs/anchors resolve, JSON-LD parses, HTML integrity 233 files/0 failures, test:copy:language PASS, tools/nurture/cta-whatsapp/public-offer-truth/article-pillar-form PASS), but two changes break CI-blocking contracts that W4 neither ran nor reported: (1) the new data-cta-id="ver-exemplo-completo" on /aditivos-obras-publicas/ raises the declared-CTA count and `npm run test:cta-form-next-state` (site-ci.yml:178 and npm `pretest`) now fails 198 !== 196 (W4 owns +1; /servicos-obras-publicas/ 10→11 is another workstream's +1); (2) /reequilibrio-obras-publicas/ meta description, og:description and JSON-LD CollectionPage.description were rewritten with no prepared replacement anywhere (only `title_og` is a prepared replacement, and proposed-replacements.json still says html_mutation_authorized:false / PREPARE-ONLY). W4's edits also moved the three protected pillars off their frozen hashes (HEAD == frozen, working tree != frozen): `test:single-commercial-route` 91/93 (CONTRACT_DEFENSE_FROZEN_DRIFT on medicoes) and 11 frozen-spec tests in `test:bofu-dominance` fail until the closing recapture chain runs. Not attributable to W4 and excluded from its ledger: bofu_buyer_decision_map source_contract_hash_drift and the `test_buyer_decision_map` failures (public-family-registry.json edited by another workstream), archetype_editorial_ready warnings, test_jobs_parity_attrs bid_room failure, and test_contact_journeys.mjs (needs `_site`, build forbidden here — not run). Commands run: python3 scripts/site/test_ai_mention_gate.py (PASS, 231 files); python3 scripts/site/test_integral_solution_copy.py (PASS 0/0); python3 scripts/site/test_html_integrity.py && python3 scripts/site/html_integrity.py --root . --surface source (PASS, failures=0); npm run test:copy:language (exit 0); npm run test:cta-form-next-state (FAIL 198!==196); npm run test:single-commercial-route (FAIL 2/93); npm run test:bofu-dominance (13 failed/104 passed); npm run test:bofu-ownership (2 failed, not W4); python3 scripts/site/inbound_gates.py (conversion ok=true, 3× debt_entry_satisfied_remove_it warn); node scripts/site/test_tools_structure.mjs, test_cta_whatsapp.mjs, test_nurture_pages.mjs, test:real-proof-registry, test:public-offer-truth, test:tools, test:conversion, test:article-pillar-form (all PASS).
 - P0 data/commercial/cta-form-next-state.v1.json 37 | `npm run test:cta-form-next-state` (site-ci.yml:178; also npm `pretest`) fails: declared CTAs 198 !== expected 196. Inventory diff vs docs/commercial/cta-form-next-state-inventory.json: /aditivos-obras-publicas/ 7→8 (+ link to /casos/aditivo-art125-demonstrativo/ with data-cta-id=ver-exemplo-completo, W4's change at aditivos-obras-publicas/index.html:101) and /servicos-obras-publicas/ 10→11 (anoth 
    FIX: Contract owner/integrator: bump `coverage.expected_declared_ctas` to the final count (198 if servicos' +1 stays) and extend `expected_declared_ctas_note` naming the aditivos 'Ver o exemplo completo, item a item' link (same wording pattern used for the medicoes +1 on 2026-09-19); then run `npm run re
 - P0 reequilibrio-obras-publicas/index.html 7 | Metadata of a frozen protected pillar mutated beyond any prepared replacement: <meta name=description> (L7), og:description (L24) and JSON-LD CollectionPage.description (L32) rewritten to 'Dossiê de reequilíbrio econômico-financeiro para a empresa contratada…'. data/bofu-dominance/frozen-specs/proposed-replacements.json:22-27 and unlock-plan.v1.json true_replacements list only `title_og` for this  
    FIX: Revert L7 meta description, L24 og:description and the CollectionPage.description in the L32 JSON-LD to the committed text (`git show HEAD:reequilibrio-obras-publicas/index.html`). Keep the <title>/og:title change only if the integrator records the title_og application in the frozen-specs recapture 
 - P1 data/bofu-dominance/frozen-specs/hashes.json 11 | W4's edits moved the three protected pillars off their frozen hashes (HEAD hashes equal the frozen baseline; working tree differs: medicoes 30f08f2c…, aditivos dd700872…, reequilibrio 1baf567b…; the three untouched pillars still match). Consequences now: `npm run test:single-commercial-route` FAIL pillar_hash_matches_live + CONTRACT_DEFENSE_FROZEN_DRIFT (scripts/commercial/render_contract_defense_ 
    FIX: Integrator closing chain after the P0s are resolved and CSS is frozen: recapture hashes.json/snapshots/specs (scripts/bofu_dominance/frozen_specs/materialize.py), approvals, canário #389, single-commercial-route and first-fold baselines on the final bytes (see memory 'CSS muda: cadeia de fechamento'
 - P1 data/organic/public-family-registry.json 550 | The three commercial-debt entries are now satisfied on-page and the conversion gate emits `debt_entry_satisfied_remove_it` (severity warn) for /casos/aditivo-art125-demonstrativo/ (L550-553), /casos/medicao-glosa-demonstrativo/ (L556-559) and /ferramentas/matriz-atraso-obra/ (L744-746), all issue #61, expires 2026-09-30. W4 correctly did not edit this file (not in its list) and reported the warn,  
    FIX: Registry owner/integrator: delete the three debt entries, re-run `python3 scripts/site/inbound_gates.py` (expect 0 debt_entry_satisfied warnings) and regenerate the bofu buyer-decision-map derived hash (`bofu_buyer_decision_map` currently reports source_contract_hash_drift on this file from another 
 - P2 aditivos-obras-publicas/index.html 101 | New sentence calls the case page 'o dossiê publicado' although /casos/aditivo-art125-demonstrativo/ is labelled 'Demonstrativo' with hypothetical numbers; wording is copied from medicoes L100 (already published), so consistency is preserved, but 'dossiê publicado' could be read as a client dossiê. 
    FIX: Optional: 'está no exemplo demonstrativo publicado' or 'está na página do exemplo' in both pillars (medicoes L100 and aditivos L101) in the same edit; not blocking.
 - P2 medicoes-glosas-obras-publicas/index.html 228 | Form-intro negatives removed in the three pillars ('não promete reversão da glosa' / 'não promete aprovação do aditivo' / 'não promete deferimento'). Material limits still stand in each Limite card (medicoes L80 'não promete recebimento', aditivos L81 'não promete deferimento', reequilibrio L80 'não antecipa deferimento') and in the visible data-form-boundary hint ('não é compra, parecer jurídico  
    FIX: None required; mention in PR evidence / recapture reason that the form-intro sentence was consolidated into the Limite card + data-form-boundary.
 - P2 ferramentas/matriz-atraso-obra/index.html 77 | Static contact block reuses `.tool-cta-contextual` (border-top + padding, assets/editorial-tool.css:98) immediately after the JS-filled `#cta` div of the same class; once a result is rendered two bordered blocks stack with a bare h2 between the result CTA and the guide. Copy itself is correct (tool organizes vs dossiê delivers; welcomes incomplete matrix; scope/location/responsibility confirmed be 
    FIX: Optional: check on the built artefact at mobile width; if the double rule looks off, move the static block below `#cta` with a `hidden`-independent wrapper or drop the border via an existing utility class. Do not edit CSS.
== a0d63f35c0a04fe40 FIX_REQUIRED — copy gates all PASS when re-run (test_ai_mention_gate: SCANNED 231, PASS; test_integral_solution_copy: PASS; test_html_integrity: HTML_INTEGRITY_TESTS_OK; html_integrity.py --root . --surface source: html_files=233 failures=0; npm run test:copy:language exit 0; test:public-offer-truth 132/132; test_form_funnel FORM_FUNNEL_OK; render_cta_form_next_state --check OK routes=31; render_contract_defense_products --check → CONTRACT_DEFENSE_PRODUCTS_OK in an isolated copy with other agents' files at HEAD, and the route forms are byte-identical). JSON-LD in all three files re-parses; the only LD change is the hub description. New hrefs resolve (#captura-contrato at servicos-obras-publicas line ~145; /auditoria-orcamento-licitacao/ and /ferramentas/prontidao-tecnica-obra-privada/ exist; h1 wording reused correctly). Form label 'Evento observado', option 'Outro evento contratual' and field 'Contexto adicional' exist as the new copy says; WhatsApp-or-e-mail is enforced client side (js/modules/form.js requireEmailOrPhone) and server side (lead-core.cjs `!telefone && !email`), and the receipt/protocol does not depend on e-mail. CSS has the nth-child(4) rule for the 4th .offer-context-item; 'sala' is defined at its first visible use. Blockers: (P0) first-fold evidence bytes for /servicos-obras-publicas/ and /bid-room-licitacoes-obras/ fail and need recapture by the integrator before publishing (npm run test:first-fold-contract → 1925/1932, FAIL evidence_/servicos-obras-publicas/_html_bytes_match and evidence_/bid-room-licitacoes-obras/_html_bytes_match — W3 disclosed this, other routes' FAILs belong to other agents); (P1) the deepened órgão paragraph publishes the scope of a catalog offer registered as WITHHELD / MODEL_ONLY / catalog_public:false with no dated publication authority found; (P1) quantitativos meta description asserts ART unconditionally. No AI mention, no jargon, no price/deadline invented, 'Exemplo demonstrativo' occurrences untouched, material limits preserved and added. Note: W3 edited scripts/commercial/render_contract_defense_products.mjs outside its listed ownership; the edit is necessary (the hub form is generator-owned and drift fails single-commercial-route) and correctly scoped to `hub`.
 - P0 servicos-obras-publicas/index.html 7 | Pinned first-fold evidence no longer matches the file bytes (also bid-room-licitacoes-obras/index.html hero). `npm run test:first-fold-contract` → FAIL evidence_/servicos-obras-publicas/_html_bytes_match and evidence_/bid-room-licitacoes-obras/_html_bytes_match (1925/1932). Any byte change to these two pinned routes blocks site-ci until recaptured; W3 could not do it in the shared tree. 
    FIX: Integrator: after all W-streams land and with CSS frozen, recapture first-fold evidence for /servicos-obras-publicas/ and /bid-room-licitacoes-obras/ (chain per project memory: approvals → frozen specs → first fold → baselines), then re-run test:first-fold-contract. Do not revert the copy to dodge t
 - P1 servicos-obras-publicas/index.html 113 | li#situacao-orgao now publishes the deliverable matrix, normative refs and ART rule of catalog offer offers[17] `public_works_technical_procurement_planning`, which is registered readiness: WITHHELD, wave_class: MODEL_ONLY, publication.catalog_public: false, html_route: null, with exclusion 'página pública #588 nesta onda'. The prior text was one sentence of situation framing. No dated authorizati 
    FIX: Either (a) record a dated founder authorization in docs/campaigns/inbound-receita-20260919/README.md for hub-level exposure of this offer and update catalog publication metadata, or (b) trim the paragraph back to situation framing plus limits: keep Lei 14.133/2021 'quando aplicável', 'não é parecer 
 - P1 quantitativos-orcamento-obras/index.html 7 | New meta description ends '…data-base conferíveis, com ART.' — unconditional ART in the search snippet, while the body qualifies it three ways ('dentro do escopo e da atribuição profissional contratados', 'somente quando o escopo exigir', 'confirmados antes do aceite técnico'). The replaced text was conditional ('Serviço de engenharia contratado, com responsável técnico'). 
    FIX: Use a conditional that fits 160 chars, e.g. 'Quantitativos, orçamento e revisão de planilha de obra pública ou privada, com origem, critério, composições e data-base conferíveis; ART no escopo contratado.' (~159 chars), or drop the ART clause.
 - P2 servicos-obras-publicas/index.html 113 | 'a ART cobre cada módulo de engenharia efetivamente produzido' is catalog-backed (technical_responsibility.art_rule) but stated as a given; catalog `responsible` is 'a confirmar antes da contratação' and AGENTS requires confirming attribution before technical acceptance. 
    FIX: 'a ART, quando aplicável, acompanha cada módulo de engenharia efetivamente produzido, após confirmação da atribuição profissional; não há ART de ato administrativo.'
 - P2 servicos-obras-publicas/index.html 113 | Acronyms DOD, DFD and ETP are not spelled out anywhere on the site (grep over all index.html: zero expansions). Plain-language gate passes, but for a first-time visitor they read as jargon. 
    FIX: Spell out once: 'documento de oficialização ou de formalização da demanda (DOD ou DFD, conforme a nomenclatura do ente), estudo técnico preliminar (ETP), termo de referência ou projeto…'.
 - P2 servicos-obras-publicas/index.html 7 | Meta/og/JSON-LD description 'Dossiês de medição e glosa, aditivo, atraso e reequilíbrio, leitura de edital e planejamento técnico da contratação para órgãos, em obras públicas.' is ambiguous: 'para órgãos' can attach to 'leitura de edital' (a licitante service), and the primary visitor (construtora/contratada) disappeared from the description. 
    FIX: e.g. 'Para construtoras: dossiês de medição e glosa, aditivo, atraso e reequilíbrio e leitura de edital; para órgãos, planejamento técnico da contratação de obras públicas.' (~150 chars); apply to the 3 identical occurrences (meta, og, JSON-LD WebPage.description).
 - P2 servicos-obras-publicas/index.html 113 | Item 10 sends órgão visitors into the contratada-side form (hidden estagio=contract-defense-products, asset_id contract-defense-products, 'Entrega mais próxima' lists only contratada dossiês; contract_event='outro'). Nothing in the payload marks the órgão side, so the conflict check promised in the copy ('o conflito é verificado antes da proposta') depends on free text in 'Contexto adicional'. 
    FIX: Add to the item-10 instruction: 'e informe em "Contexto adicional" que fala pelo órgão contratante' (no new field), and note in docs/ops/WARMBLY-INBOUND.md that contract_event=outro from route_family servicos-obras-publicas may be an órgão lead requiring the conflict gate.
== a791e5482eee33282 FIX_REQUIRED — nothing W6 changed is unsafe as committed today (all claimed gates re-run and PASS: test_ai_mention_gate 231 files PASS; test_integral_solution_copy PASS; test_html_integrity + html_integrity --surface source failures=0; test:copy:language PASS; test:tools PASS incl. ferramentas_capture_forms_bound_by_runtime (3 forms) and counter-case; test:nurture + test_nurture_pages PASS; test_lead_stages + test_privacy PASS; test_apply_form_nojs_note PASS; test:form-funnel PASS). Verified clean: hidden form-name="checklist-reequilibrio" survives the rename (form.js:502 only defaults when absent), applyJourneyToForm cannot throw on the checklist page (#estagio hidden with value, zero data-set-journey), publicLeadSummary is only served behind authOps in ops.cjs and the new fields are provider handles (no PII; revenue_daily.mjs:126 still reads del.email as a plain status), nurture native POST reaches the handler (Origin/Referer fallback, urlencoded parsed, consent 'on' accepted), the noscript note does not depend on home-10x.css (only the home loads it). Two P1s block: (1) apply_form_nojs_note.py's exclusion set ignores the six frozen-specs pillars, so the intended next step (--write) would break the frozen-hash gate on auditoria-orcamento-licitacao and diagnostico-b2g-360, which are currently byte-identical to the baseline; (2) the no-JS note itself was never applied — 16 capture forms still carry nothing (only the home has a note), so the visitor-facing deliverable is undelivered. NOTE: apply_form_nojs_note.py changed on disk mid-review (GENERATOR_OWNED added; --check went 30 → 16 pending); findings are as of the second read. Also observed, NOT attributable to W6: npm run test:cta-form-next-state fails (declared CTAs 198 !== 196) because /aditivos-obras-publicas/ (7→8) and /servicos-obras-publicas/ (10→11) gained CTAs from other workstreams — integrator must recapture expected_declared_ctas.
 - P1 scripts/site/apply_form_nojs_note.py 158 | excluded() only filters font_preload.hash_bound_pages()/BYTE_PINNED_ARTICLES (four conteudos/ articles), piloto/, nurture/, index.html and GENERATOR_OWNED. It does not exclude the six pillars frozen in data/bofu-dominance/frozen-specs/hashes.json. `--check` lists aditivos-obras-publicas, medicoes-glosas-obras-publicas, reequilibrio-obras-publicas, auditoria-orcamento-licitacao and diagnostico-b2g- 
    FIX: Read the pillar keys from data/bofu-dominance/frozen-specs/hashes.json (`pillars`) and add `<key>/index.html` to the exclusion set (or require an explicit `--include-frozen` flag that the integrator runs together with the recapture chain); add a fixture test that a frozen pillar is left byte-identic
 - P1 scripts/site/apply_form_nojs_note.py 1 | Scope not delivered: the applier was written and tested but never run, so no capture form outside the home carries the no-JS note (grep form-nojs-note over visitor HTML = 1 file, the home). `--check` exits 1 with 16 pending pages, including the three ferramentas forms W6 owns (checklist-reequilibrio, diagnostico-defesa-margem, limite-acrescimos-supressoes). The self-report discloses this ('not yet 
    FIX: After fixing the frozen-pillar exclusion, run `python3 scripts/site/apply_form_nojs_note.py --write` on the W6-owned ferramentas pages now, and hand the remaining 13 (pillars/service pages owned by other workstreams) to the integrator with the recapture chain; re-run html_integrity --surface source 
 - P2 scripts/site/apply_form_nojs_note.py 62 | Comment says the generator-owned pendency is 'registrada em docs/campaigns/bofu-integral-20260919/README.md', but neither that README nor docs/campaigns/inbound-receita-20260919/README.md mentions nojs/noscript/W6 (grep empty). Dangling citation for the justification of GENERATOR_OWNED. 
    FIX: Either add the pendency entry (generator list: live_intelligence/render.py, offers/contractual_claims.cjs, render_licitacao_products.mjs, render_public_catalog.mjs, render_contract_defense_products.mjs, offers/render.cjs) to the README or change the comment to point where it is actually recorded.
 - P2 nurture/index.html 126 | noscript copy says 'este formulário não mostra a tela de confirmação. Use o e-mail … para pedir a sequência', but with the new method/action the native POST still succeeds (handler parses urlencoded, consent 'on' accepted, returns 201 pending_confirm as raw JSON and sends the confirmation e-mail). A no-JS visitor who follows the note both submits and e-mails, creating a duplicate request; and the  
    FIX: Rephrase to describe what actually happens, e.g.: 'Sem JavaScript, o envio funciona, mas a resposta aparece como texto técnico; a confirmação chega por e-mail. Se preferir, peça a sequência por e-mail <a>…</a>, informando a trilha.'
 - P2 ferramentas/checklist-reequilibrio/index.html 91 | Form renamed to the legacy runtime name `diagnostico-confenge` while the two sibling ferramentas capture forms (diagnostico-defesa-margem, limite-acrescimos-supressoes) and data/site/brand.json (`form.name: diagnostico-b2g`, `legacy_name: diagnostico-confenge`) use the canonical `diagnostico-b2g`. Functionally equivalent (same selector in nav.js:584) but inconsistent with the canonical contract. 
    FIX: Use name="diagnostico-b2g" for consistency with brand.json and the sibling tools; test still passes.
 - P2 scripts/site/apply_form_nojs_note.py 125 | The fieldset insertion path places <noscript><p> immediately after the opening <fieldset>, i.e. before <legend>; the HTML content model requires legend to be the first child of fieldset, so this path produces invalid markup (test position_after_opening_fieldset pins the behaviour). No current target uses this path (all 16 pending hit the form-hint or bare-form path), so it is latent. 
    FIX: When a <legend> follows the <fieldset> open tag, insert after </legend> instead; adjust the test fixture accordingly.
 - P2 scripts/site/apply_form_nojs_note.py 43 | Note text 'Sem JavaScript, este formulário não envia. Use o WhatsApp ou o e-mail ao lado.' — 'ao lado' is misleading (the links are inside the sentence itself, not beside the form on most pages), and the term 'JavaScript' departs from the plainer phrasing the home already publishes ('Neste navegador, o formulário não consegue registrar o pedido nem devolver um protocolo…'). Truthfulness holds in p 
    FIX: Reuse the home wording pattern: 'Neste navegador, o formulário não consegue registrar o pedido nem devolver um protocolo. Fale pelo <a>WhatsApp</a> ou pelo <a>e-mail</a>: chegam direto e valem o mesmo.'
 - P2 scripts/site/test_tools_structure.mjs 290 | runtime_selector_counter_case only asserts that the literal 'checklist-reequilibrio' is absent from the nav.js selector — a constant that can never be true given the selector; it does not exercise the transform on a fixture with the old name, so it adds no discrimination beyond the main assertion. 
    FIX: Run the check function against an inline fixture form tag with name="checklist-reequilibrio" and assert it is reported unbound; or drop the case.
== a0a2fdc77e37bacfc FIX_REQUIRED — Visitor-facing edits (4 HTML files) are clean: pt-BR ok, no AI mention, no invented claims, material limits preserved (revisão L117 keeps the checklist/norma limit; casos keeps the data-form-boundary hint and the client-work-in-its-own-section distinction at L124-125), JSON-LD valid in all four pages, html_integrity source surface 233 files / 0 failures, test_ai_mention_gate PASS (231 files), test_integral_solution_copy PASS, npm run test:copy:language PASS, validate_seo VALIDATION_OK, test:visible-parity exit 0, test_inb05 x2 + test_page_contract pass 30/fail 0. The blocking issue is in the data half: docs/seo/URL-DISPOSITION-MATRIX.csv was hand-edited but its twin docs/seo/URL-DISPOSITION-MATRIX.json (the artifact actually read by scripts/site/test_inbound_gates.py:698 and :719, and written together with the CSV by inbound_first_remediate.py:2099-2100) was not, so the matrix now contradicts itself (154 vs 162 rows; chuva row noindex/RETAIN_NOINDEX in JSON vs index/KEEP_AND_IMPROVE in CSV). build_inventory({}) reproduces W5's values (chuva index,follow / KEEP_AND_IMPROVE, in_sitemap/in_hub True; the 8 /conteudos/ private-project rows with empty cluster), so the fix is to sync the JSON (hand-sync the 9 rows in the same shape, or regenerate both halves via the generator's writer — never via the inbound_first_remediate CLI, which rewrites ~160 pages). Also: the self-report's 'single failure, all other inbound-gates tests OK' was not true at review time (5 FAIL: canary-389 on medicoes-glosas, registered-debt route-exact, exempt-route report on /ferramentas/matriz-atraso-obra/, bofu buyer-decision-map hash drift x2); all trace to files owned by other agents in the shared tree, none to W5's six files. No silently skipped items detected beyond the compat card (renderer-owned, byte-equal contract, correctly left alone). No hrefs/anchors were added.
 - P1 docs/seo/URL-DISPOSITION-MATRIX.json 753 | The disposition matrix's authoritative half was not updated: JSON still has 154 rows with /conteudos/chuva-prorrogacao-prazo-obra-publica/ as robots 'noindex,follow' / disposition RETAIN_NOINDEX and lacks the 8 /conteudos/ private-project rows that the CSV (162 rows) now carries. test_inbound_gates.py reads only the JSON (lines 698, 719); inbound_first_remediate.py writes both files from the same  
    FIX: Sync docs/seo/URL-DISPOSITION-MATRIX.json with the same 9 row changes (append the 8 KEEP_AND_IMPROVE rows and update the chuva row's robots/in_sitemap/in_hub/title/search_intent/disposition/notes), or regenerate both files through the generator's writer function. Do not invoke the inbound_first_reme
 - P1 scripts/site/test_inbound_gates.py 1 | Self-report claims a single inbound-gates failure with 'all other inbound-gates tests OK'. Re-run at review time shows 5 FAIL: test_measurement_delay_canary_389_is_single_url_and_fail_closed (medicoes-glosas-obras-publicas), test_registered_debt_is_route_exact_and_never_absorbs_a_sibling, test_report_lists_every_exempt_route_with_its_reason (/ferramentas/matriz-atraso-obra/), test_run_all_gates_ok 
    FIX: Integrator: re-run python3 scripts/site/test_inbound_gates.py after all workstreams land and recapture the buyer-decision-map source hash for data/organic/public-family-registry.json; W5 report should state the actual failure set observed.
 - P2 docs/seo/URL-DISPOSITION-MATRIX.csv 32 | Note on the chuva row says 'index,follow em disco desde o canário de 2026-09-09', but git history shows the article returned to index,follow in commit 7500d7bde on 2026-08-29 ('reescrever e indexar canário de chuva em striking distance (#491)'); d068afee8 (2026-09-19) only touched the footer. 
    FIX: Change the note to 'index,follow em disco desde 2026-08-29 (7500d7bde, #491)'.
 - P2 casos/index.html 10 | Added Open Graph set (og:type, og:url, og:title, og:description) is incomplete against the sibling hub pattern: servicos/index.html also carries og:locale=pt_BR and og:site_name=CONFENGE, and scripts/organic/serp_ctr.py:153 reads og:site_name. 
    FIX: Add <meta property="og:locale" content="pt_BR"/> and <meta property="og:site_name" content="CONFENGE"/> next to the other og tags.
 - P2 docs/seo/URL-DISPOSITION-MATRIX.csv 163 | File is CRLF-terminated (162 lines) but the final appended row ends with a bare LF; HEAD also had exactly one bare-LF line, so the count is unchanged, but the odd line moved to the new last row. 
    FIX: Terminate the last row with CRLF (or normalize the whole file in a dedicated commit) so the generator's output and the hand-edited file diff cleanly.
 - P2 seo/visible-parity.json 566 | Build output still holds the four old meta descriptions (lines 566, 1376, 4776, 5252). Not a W5 defect (build:site output, integrator-owned) and test:visible-parity currently exits 0; noted so the integrator's rebuild is expected to change this file. 
    FIX: No action for W5; integrator regenerates via build:site / parity:site.
