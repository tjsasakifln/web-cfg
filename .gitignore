.DS_Store
Thumbs.db
*.log
.env
.env.*
!.env.example
.vscode/
.idea/
node_modules/
.cache/
.netlify/
*.swp
*~

.playwright-mcp/

__pycache__/

# Generated at build/deploy (Netlify npm run build:site). Do not self-hash-commit.
.well-known/pseo-build.json
.well-known/build-info.json
.well-known/release-result.json
.well-known/build-manifest.json
seo/REPRODUCIBLE-BUILD-MANIFEST.json

# Isolated public artifact (Netlify publish directory)
_site/

# Lighthouse full runs (keep summary only)
docs/lighthouse-runs/*.json
!docs/lighthouse-runs/summary.json


# Chrome/Lighthouse temp profiles (Windows path leaks)
C:/
**/lighthouse.*/


# Chrome/Lighthouse temp profiles (Windows path leaks under repo)
C:/
**/lighthouse.*/

# Ephemeral schedule run proofs (CI artifacts)
data/revops/schedule-runs/
data/revops/dsar/
data/revops/exports/
storage-dumps/
storage-backups/
*.confenge-storage-dump
data/discovery/receipts/*.json
data/revops/gsc/last_sync.json
data/revops/gsc/history.json
data/revops/gsc/latest_import.json
# Keep the already-tracked historical snapshot visible to rg/audit.
!data/revops/gsc/latest_import.json
data/revops/gsc/daily/
data/revops/gsc/snapshots/*/query-page.json
data/revops/gsc/private/
docs/ops/campaigns/**/private/

# Reversa
.reversa/config.user.toml
_reversa_sdd/

# Environment & Secrets (AIOX)
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules

# Build & Logs (AIOX)
dist/
build/
logs/

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Design-direction capture binaries (#494). The per-file SHA-256 index in
# docs/design-audit/evidence/capture-index.json is the durable evidence; the
# 28 MB of PNG are reproducible with scripts/site/capture_design_direction.sh.
docs/design-audit/evidence/capture/**/*.png

# Official Live Intelligence producer input is delivered at the host, not committed.
data/live_intelligence/official/
data/live_intelligence/official.private/
# Official consumer projection and last-accepted rollback snapshot.
data/live_intelligence/accepted/
data/live_intelligence/accepted.last/
