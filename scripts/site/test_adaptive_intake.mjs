// The campaign contract tests live with the intake fixtures so they can run
// both through node:test and the repository's historical npm script.
await import("../../tests/intake/test_mv03_adaptive_intake.mjs");
